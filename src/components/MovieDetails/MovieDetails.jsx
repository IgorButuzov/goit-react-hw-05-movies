// import { useState,useEffect } from "react";

// import getFromApi from "../fetch";

const MovieDetails = (movieId) => {
    // const [movieId,setMovieId] = useState('');
    
    // useEffect(()=>{
    //   getFromApi.movieDetails()
    // .then(data => setMovieId(data.results))
    // },[])
  
    return (
        // movieId.map(({backdrop_path}) => (
        //     <li >
        //     <img src={`https://image.tmdb.org/t/p/w500/${backdrop_path}`} alt={''} width={400} />
        // </li> 
        // ))
        <div>`movieId details`</div>
        
    );
}

export default MovieDetails;